apiVersion: v1
kind: Pod
metadata:
  name: test-pod-safe
  namespace: default
  labels:
    app: test-pod-safe
spec:
  containers:
    - name: nginx
      image: nginx:alpine
      ports:
        - containerPort: 80
      env:
        - name: ENVIRONMENT
          value: "production"
